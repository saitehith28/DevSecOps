for i in $ls; do cd $i; terraform init; terraform apply -auto-approve; cd ..; done

for i in 00-vpc/ 10-sg/ 20-sg-rules/ 30-bastion/; do cd $i; terraform init; terraform apply -auto-approve; cd ..; done

for i in 00-vpc/ 10-sg/ 20-sg-rules/ 30-bastion/; do cd $i; terraform apply -auto-approve; cd ..; done


git config --global credential.helper store
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

git clone https://github.com/saitehith28/DevSecOps.git
cd DevSecOps/roboshop-infra-dev

Make the repo public it will not ask credentials