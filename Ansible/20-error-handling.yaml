- name: demo on error handling
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: check iuser exist or not
    ansible.builtin.command: id roboshop
    register: user_output
    ignore_erros: true

  - name: print the output
    ansible.builtin.debug:
      msg: "{{ user_output }}"